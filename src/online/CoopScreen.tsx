import React from 'react'
import { Copy, LogOut, Map, Users, Wifi, WifiOff } from 'lucide-react'
import { HEROES, SUBREGIONS, TERRITORIES, useGame } from '../store/game'
import { normalizeRoomCode, onlineConfigured } from './supabase'
import { useCoop } from './CoopContext'

const art=(hero:any)=>'./'+(hero?.arte??hero?.imagem??'')
export default function PersistentCoopScreen(){
 const g=useGame(),coop=useCoop(),[name,setName]=React.useState(()=>localStorage.getItem('bangalores-coop-name')??''),[code,setCode]=React.useState('')
 const {room,members,userId,onlineCount,busy,notice}=coop,me=members.find(member=>member.user_id===userId),isHost=room?.host_id===userId
 const destination=room?.shared_state?.destination as {regionId?:string;subregionId?:string}|undefined,regionId=destination?.regionId??g.regionId,subregions=SUBREGIONS.filter(sub=>sub.regionId===regionId),selectedSub=SUBREGIONS.find(sub=>sub.id===destination?.subregionId)
 return <div className="coop-page"><section className="panel coop-hero"><div><span className="eyebrow">BANGALORE'S ONLINE</span><h1>Cooperativo em tempo real</h1><p>A sala permanece conectada enquanto você usa os outros menus do jogo.</p></div>{onlineConfigured?<Wifi className="online"/>:<WifiOff className="offline"/>}</section>{!onlineConfigured?<section className="panel coop-setup"><WifiOff/><h2>Serviço online ainda não conectado</h2></section>:!room?<div className="coop-entry"><section className="panel"><h2>Seu aventureiro</h2><label>Nome visível<input maxLength={24} value={name} onChange={event=>setName(event.target.value)} placeholder="Nome do jogador"/></label></section><section className="panel"><h2>Criar ou entrar</h2><button className="primary" disabled={busy||!name.trim()} onClick={()=>void coop.create(name,g.heroId)}><Users/>Criar sala cooperativa</button><div className="coop-divider"><span/>OU<span/></div><label>Código da sala<input value={code} onChange={event=>setCode(normalizeRoomCode(event.target.value))} maxLength={6} placeholder="ABC123"/></label><button disabled={busy||!name.trim()||code.length!==6} onClick={()=>void coop.join(code,name,g.heroId)}>Entrar na sala</button>{notice&&<p className="coop-notice">{notice}</p>}</section></div>:<><section className="panel coop-room"><header><div><small>CÓDIGO DA SALA</small><strong>{room.code}</strong></div><button onClick={()=>navigator.clipboard?.writeText(room.code)}><Copy/>Copiar código</button><span className="coop-live"><Wifi/> {onlineCount||members.length} online</span><button className="danger-action" disabled={busy} onClick={()=>void coop.leave()}><LogOut/>Sair da sala</button></header><div className="coop-members">{members.map((member,index)=>{const hero=HEROES.find(item=>item.id===member.hero_id),host=member.user_id===room.host_id;return <article className={host?'coop-host':''} key={member.id}><span>{index+1}</span>{hero&&<img src={art(hero)} alt=""/>}<div><small>{host?'ANFITRIÃO • ESCOLHE O DESTINO':'AVENTUREIRO'}</small><strong>{member.display_name}{member.user_id===userId?' (você)':''}</strong><em>{hero?.nome??'Herói não escolhido'}</em></div><b className={member.ready?'ready':''}>{member.ready?'PRONTO':'PREPARANDO'}</b></article>})}</div><footer><p>Cada jogador mantém e atualiza sua própria campanha. A experiência será dividida proporcionalmente ao dano causado por cada herói.</p><button className="primary" disabled={!me||busy} onClick={()=>void coop.toggleReady(g.heroId)}>{me?.ready?'Cancelar prontidão':'Marcar como pronto'}</button></footer></section><section className="panel coop-destination"><h2>Destino compartilhado</h2><p>{isHost?'Selecione a região e a sub-região para o grupo.':'O anfitrião está escolhendo o destino da aventura.'}</p><label>Região<select disabled={!isHost||busy} value={regionId} onChange={event=>{const first=SUBREGIONS.find(sub=>sub.regionId===event.target.value);if(first)void coop.selectDestination(event.target.value,first.id)}}>{TERRITORIES.map(region=><option key={region.id} value={region.id}>{region.nome}</option>)}</select></label><div className="coop-destination-grid">{subregions.map(sub=><button className={sub.id===destination?.subregionId?'selected':''} disabled={!isHost||busy} key={sub.id} onClick={()=>void coop.selectDestination(sub.regionId,sub.id)}>{sub.nome}<small>Nível {sub.nivelMin}–{sub.nivelMax}</small></button>)}</div>{selectedSub&&<p className="coop-selected"><Map/> Destino atual: <strong>{selectedSub.nome}</strong></p>}</section></>}</div>
}

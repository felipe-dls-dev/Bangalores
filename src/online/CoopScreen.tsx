import React from 'react'
import { Copy, LogOut, Map, Users, Wifi, WifiOff } from 'lucide-react'
import { buildCoopEnemy, HEROES, levelInfo, SUBREGIONS, TERRITORIES, useGame } from '../store/game'
import type { Enemy } from '../types'
import { normalizeRoomCode, onlineConfigured } from './supabase'
import { useCoop } from './CoopContext'
const art=(hero:any)=>'./'+(hero?.arte??hero?.imagem??'')
export default function PersistentCoopScreen(){
 const g=useGame(),coop=useCoop(),[name,setName]=React.useState(()=>localStorage.getItem('bangalores-coop-name')??''),[code,setCode]=React.useState(''),startedBattle=React.useRef('')
 const {room,members,userId,onlineCount,busy,notice}=coop,me=members.find(member=>member.user_id===userId),isHost=room?.host_id===userId,destination=room?.shared_state?.destination as {regionId?:string;subregionId?:string}|undefined
 const regionId=destination?.regionId??g.regionId,subregions=SUBREGIONS.filter(sub=>sub.regionId===regionId),selectedSub=SUBREGIONS.find(sub=>sub.id===destination?.subregionId),accepted=Array.isArray(room?.shared_state?.travelAcceptedBy)?room!.shared_state.travelAcceptedBy as string[]:[],acceptedMe=accepted.includes(userId),battle=room?.shared_state?.battle as {id?:string;subregionId?:string;enemy?:Enemy}|undefined
 React.useEffect(()=>{if(!battle?.id||!battle.enemy||!battle.subregionId||startedBattle.current===battle.id)return;startedBattle.current=battle.id;g.startCoopCombat(battle.enemy,battle.subregionId)},[battle?.id,battle?.enemy,battle?.subregionId,g.startCoopCombat])
 return <div className="coop-page"><section className="panel coop-hero"><div><span className="eyebrow">BANGALORE'S ONLINE</span><h1>Cooperativo em tempo real</h1><p>A sala permanece conectada enquanto você usa os outros menus.</p></div>{onlineConfigured?<Wifi className="online"/>:<WifiOff className="offline"/>}</section>{!onlineConfigured?<section className="panel coop-setup"><WifiOff/><h2>Serviço online ainda não conectado</h2></section>:!room?<div className="coop-entry"><section className="panel"><h2>Seu aventureiro</h2><label>Nome visível<input maxLength={24} value={name} onChange={e=>setName(e.target.value)}/></label></section><section className="panel"><h2>Criar ou entrar</h2><button className="primary" disabled={busy||!name.trim()} onClick={()=>void coop.create(name,g.heroId)}><Users/>Criar sala cooperativa</button><div className="coop-divider"><span/>OU<span/></div><label>Código da sala<input value={code} onChange={e=>setCode(normalizeRoomCode(e.target.value))} maxLength={6}/></label><button disabled={busy||!name.trim()||code.length!==6} onClick={()=>void coop.join(code,name,g.heroId)}>Entrar na sala</button>{notice&&<p className="coop-notice">{notice}</p>}</section></div>:<><section className="panel coop-room"><header><div><small>CÓDIGO DA SALA</small><strong>{room.code}</strong></div><button onClick={()=>navigator.clipboard?.writeText(room.code)}><Copy/>Copiar código</button><span className="coop-live"><Wifi/> {onlineCount||members.length} online</span><button className="danger-action" onClick={()=>void coop.leave()}><LogOut/>Sair</button></header><div className="coop-members">{members.map((member,index)=>{const hero=HEROES.find(item=>item.id===member.hero_id),host=member.user_id===room.host_id;return <article className={host?'coop-host':''} key={member.id}><span>{index+1}</span>{hero&&<img src={art(hero)} alt=""/>}<div><small>{host?'ANFITRIÃO • ESCOLHE O DESTINO':'AVENTUREIRO'}</small><strong>{member.display_name}{member.user_id===userId?' (você)':''}</strong><em>{hero?.nome}</em></div><b className={member.ready?'ready':''}>{member.ready?'PRONTO':'PREPARANDO'}</b></article>})}</div><footer><p>Cada jogador atualiza sua campanha; a experiência será proporcional ao dano causado.</p><button className="primary" disabled={!me||busy} onClick={()=>void coop.toggleReady(g.heroId)}>{me?.ready?'Cancelar prontidão':'Marcar como pronto'}</button></footer></section><section className="panel coop-destination"><h2>Destino compartilhado</h2><p>{isHost?'Selecione o destino. Todos deverão confirmar a viagem.':'Aguarde a proposta do anfitrião e confirme a viagem.'}</p><label>Região<select disabled={!isHost||busy} value={regionId} onChange={e=>{const first=SUBREGIONS.find(sub=>sub.regionId===e.target.value);if(first)void coop.selectDestination(e.target.value,first.id)}}>{TERRITORIES.map(region=><option key={region.id} value={region.id}>{region.nome}</option>)}</select></label><div className="coop-destination-grid">{subregions.map(sub=><button className={sub.id===destination?.subregionId?'selected':''} disabled={!isHost||busy} key={sub.id} onClick={()=>void coop.selectDestination(sub.regionId,sub.id)}>{sub.nome}<small>Nível {sub.nivelMin}–{sub.nivelMax}</small></button>)}</div>{selectedSub&&<><p className="coop-selected"><Map/> Destino proposto: <strong>{selectedSub.nome}</strong></p><div className="coop-travel-confirm"><span><strong>{accepted.length}/{members.length} confirmações</strong><small>{acceptedMe?'Sua viagem está confirmada.':'Confirme para viajar com o grupo.'}</small></span><button className="primary" disabled={busy||acceptedMe||members.length<2} onClick={()=>void coop.confirmTravel(buildCoopEnemy(selectedSub.id,levelInfo(g.xp).lvl,g.difficultyMode) as unknown as Record<string,unknown>)}>{acceptedMe?'Viagem confirmada':'Confirmar viagem'}</button></div></>}</section></>}</div>
}
